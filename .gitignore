._*

node_modules/

*.galaxy.*
version.desktop

.desktop
.desktop/*

logs
logs/*

test
tests_output
tests/output
tests/screenshots
tests/nightwatch/screenshots
tests_output

# npmPackages/* and core/* are tracked in this monorepo — npmPackages is the
# workflow-package home (the old packages/ Atmosphere dir is retired to deprecated/).
#
# extensions/* are private/user-defined packages, each with its own nested git
# repo — ignore their contents, keep only the directory-level CLAUDE.md stub so
# the directory stays in the tree. Nothing under extensions/ is checked into this
# monorepo.
extensions/*
!extensions/CLAUDE.md
!extensions/API.md

# Retired Atmosphere packages parked post-migration (each keeps its own nested git repo)
deprecated/

.DS_Store

# Environment files (contain secrets)
.env
.env.*
!.env.*.example

.meteor/packages

settings.*.json
# ...but tracked secret-free templates (*.template.json) ship with their package.
# These MUST contain no real secrets — secret values are empty, filled via env or a
# local (gitignored) settings.*.json copy at runtime.
!settings.*.template.json
# accounts.multiuser.settings.json doesn't match the settings.*.json basename
# glob — it was force-tracked with a live admin password until CR-6/HI-9
# (2026-07-24). Its scrubbed .template.json copy doesn't match this pattern
# and tracks normally.
accounts.*.settings.json

screenshots/*
screenshots/*/*

scrap
workzone
fable

proprietary
contracted

# Meteor Modern-Tools build context directories
_build
*/build-assets
*/build-chunks
.rsdoctor

# Auto-generated workflow barrels (generated by rspack.workflowParser.js)
imports/workflows/index.js
imports/workflows/loader.js
imports/workflows/server-loader.js

# Auto-generated workflow assets (copied by rspack.workflowParser.js)
public/workflows/

# CesiumJS static assets (copied by scripts/copy-cesium-assets.js)
public/cesium/


# Ralph loop ephemeral state
.claude/ralph-loop.local.md

# eCQM measure bundles (MADiE exports) — autoloaded at startup by
# quality-measures. Contain VSAC value-set expansions (UMLS-licensed,
# non-redistributable) so they stay per-deployment, never in git.
measure-bundles/

# Local tool outputs and scratch artifacts
# (graphify-out/ is regenerated on demand; a committed stale graph lies with confidence)
.playwright-mcp/
graphify-out/
# scratch under libraries/ is ignored, but the dcmjs submodule gitlink is tracked
libraries/*
!libraries/dcmjs
/hero-pad.png
/lunar-homepage-intro.png

# certification manual build intermediates (staged by select-inferno-report.js)
certification/inferno-reports/latest-g10-summary.pdf
certification/inferno-report-meta.tex
