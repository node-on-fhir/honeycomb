version: 2.1

orbs:
  browser-tools: circleci/browser-tools@1.4.8

# Define test groups for parallelization
parameters:
  test-groups:
    type: string
    default: |
      [
        {
          "name": "accounts",
          "tests": [
            "tests/nightwatch/honeycomb/default/accounts.login.js",
            "tests/nightwatch/honeycomb/default/accounts.signup.js",
            "tests/nightwatch/honeycomb/default/accounts.viewports.js"
          ]
        },
        {
          "name": "actors",
          "tests": [
            "tests/nightwatch/honeycomb/enable_autopublish/crud.patients.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.practitioners.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.practitionerRoles.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.organizations.js"
          ]
        },
        {
          "name": "clinical-history",
          "tests": [
            "tests/nightwatch/honeycomb/enable_autopublish/crud.observations.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.conditions.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.encounters.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.procedures.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.bodystructures.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.clinicalimpressions.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.riskassessments.js"
          ]
        },
        {
          "name": "autoimmune",
          "tests": [
            "tests/nightwatch/honeycomb/enable_autopublish/crud.allergyintolerances.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.immunizations.js"

          ]
        },
        {
          "name": "pharmacy",
          "tests": [
            "tests/nightwatch/honeycomb/enable_autopublish/crud.medications.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.medicationrequests.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.medicationadministrations.js"
          ]
        },
        {
          "name": "radiology",
          "tests": [
            "tests/nightwatch/honeycomb/enable_autopublish/crud.diagnosticreports.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.imagingstudies.js"
          ]
        },
        {
          "name": "care-management",
          "tests": [
            "tests/nightwatch/honeycomb/enable_autopublish/crud.careplans.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.nutritionintakes.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.nutritionorders.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.nutritionProducts.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.servicerequests.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.activitydefinitions.js"
          ]
        },
        {
          "name": "structured-data-capture",
          "tests": [
            "tests/nightwatch/honeycomb/enable_autopublish/crud.questionnaires.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.questionnaireresponses.js"
          ]
        },
        {
          "name": "clinical-trials",
          "tests": [
            "tests/nightwatch/honeycomb/enable_autopublish/crud.researchstudy.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.researchsubjects.js"
          ]
        },
        {
          "name": "administrative",
          "tests": [
            "tests/nightwatch/honeycomb/enable_autopublish/crud.auditevents.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.consents.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.tasks.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.plandefinitions.js"
          ]
        },
        {
          "name": "supply-chain",
          "tests": [
            "tests/nightwatch/honeycomb/enable_autopublish/crud.locations.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.devices.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.supplydeliveries.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.supplyRequests.js"
          ]
        },
        {
          "name": "communications",
          "tests": [
            "tests/nightwatch/honeycomb/enable_autopublish/crud.communications.js"
            "tests/nightwatch/honeycomb/crud.messageheaders.js"
          ]
        },
        {
          "name": "public-health",
          "tests": [
            "tests/nightwatch/honeycomb/crud.measures.js",
            "tests/nightwatch/honeycomb/crud.measurereports.js"
          ]
        },
        {
          "name": "library-sciences",
          "tests": [
            "tests/nightwatch/honeycomb/enable_autopublish/crud.documentreferences.js"
          ]
        },
        {
          "name": "framework",
          "tests": [
            "tests/nightwatch/honeycomb/enable_autopublish/simple-sorting-test.js",
            "tests/nightwatch/honeycomb/dev_auto_login/my.profile.js",
            "tests/nightwatch/honeycomb/default/localhost.js",
            "tests/nightwatch/honeycomb/enable_autopublish/crud.endpoints.js"
          ]
        },
        {
          "name": "base-ehr",
          "tests": [
            "npmPackages/order-catalog/tests/nightwatch/170.315.a.1.test.js",
            "npmPackages/order-catalog/tests/nightwatch/170.315.a.2.test.js",
            "npmPackages/implantable-devices/tests/nightwatch/170.315.a.14.test.js",
            "npmPackages/hipaa-compliance/tests/nightwatch/170.315.d.1.test.js",
            "npmPackages/family-health-history/tests/nightwatch/170.315.a.12.test.js"
          ]
        }
      ]

jobs:
  # Job template that runs a specific group of tests
  test-group:
    parameters:
      group-name:
        type: string
      test-files:
        type: string
      disable-auto-login:
        type: boolean
        default: false
      # npm workflow packages to load via EXTRA_WORKFLOWS (replaces the old
      # Atmosphere `--extra-packages "clinical:..."` mechanism). Core CRUD groups
      # only need the app to boot; ONC-cert groups override with their packages.
      extra-workflows:
        type: string
        default: "@node-on-fhir/reference-app,@node-on-fhir/us-core,@node-on-fhir/pacio-core"
    docker:
      - image: cimg/node:20.11.0-browsers
        environment:
          CIRCLE_ARTIFACTS: /tmp/circleci-artifacts
          CIRCLE_TEST_REPORTS: /tmp/circleci-test-results
          TZ: "America/Chicago"
          NODE_ENV: test
          NIGHTWATCH: true
          DISPLAY: :99.0
          NODE_OPTIONS: --max_old_space_size=2048
          TOOL_NODE_FLAGS: --max_old_space_size=2048
          TEST_BROWSER_DRIVER: chrome
          MONGO_URL: mongodb://mongo:27017/meteor_test
          ENABLE_AUTOPUBLISH: true
          DEV_AUTO_LOGIN: true
          DEV_AUTO_USERNAME: "demouser"
          DEV_AUTO_PASSWORD: "password2025"
          TEST_RUN: true
          METEOR_DISABLE_OPTIMISTIC_CACHING: 1
          METEOR_PROFILE: 100
      - image: mongo:6.0
        name: mongo
    
    resource_class: large  # 8GB - NODE_OPTIONS (2GB) leaves 6GB for MongoDB + Chrome + pacio-core
    parallelism: 1  # Ensure we're not trying to parallelize within the job
    
    steps:
      - checkout

      - run:
          name: Init dcmjs submodule
          # The root "dcmjs": "file:libraries/dcmjs" dependency needs the
          # submodule present or npm install fails at the missing package.json
          command: git submodule update --init libraries/dcmjs

      - restore_cache:
          keys:
            - meteor-install-v3.3-{{ arch }}
            - meteor-install-v3.3-

      - restore_cache:
          keys:
            - npm-deps-v4-{{ checksum "package-lock.json" }}
            - npm-deps-v4-

      - run:
          name: Install Chrome with retry
          command: |
            # Update package lists with retry
            for i in {1..3}; do
              echo "Attempt $i to update package lists..."
              sudo apt-get update && break || sleep 10
            done
            
            # Install Chrome dependencies and tools
            sudo apt-get install -y wget gnupg jq unzip curl
            
            # Add Chrome repository
            wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
            echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
            
            # Update again and install Chrome with retry
            for i in {1..3}; do
              echo "Attempt $i to install Chrome..."
              sudo apt-get update && sudo apt-get install -y google-chrome-stable && break || {
                echo "Failed attempt $i, cleaning apt cache..."
                sudo apt-get clean
                sudo rm -rf /var/lib/apt/lists/*
                sleep 10
              }
            done
            
            # Verify installation
            google-chrome --version || {
              echo "Chrome installation failed!"
              exit 1
            }
      
      - run:
          name: Install ChromeDriver
          command: |
            # Get Chrome version
            CHROME_VERSION=$(google-chrome --version | awk '{print $3}')
            echo "Chrome version: $CHROME_VERSION"
            
            # Use Chrome for Testing endpoints for newer Chrome versions
            CHROME_MAJOR=$(echo $CHROME_VERSION | cut -d'.' -f1)
            
            if [ "$CHROME_MAJOR" -ge "115" ]; then
              # Use new Chrome for Testing API
              echo "Using Chrome for Testing API for Chrome $CHROME_MAJOR"
              
              # Get the latest ChromeDriver version for this Chrome version
              CHROMEDRIVER_URL=$(curl -s "https://googlechromelabs.github.io/chrome-for-testing/known-good-versions-with-downloads.json" | \
                jq -r ".versions[] | select(.version | startswith(\"$CHROME_VERSION\")) | .downloads.chromedriver[] | select(.platform==\"linux64\") | .url" | \
                head -n 1)
              
              if [ -z "$CHROMEDRIVER_URL" ]; then
                # Fallback to latest ChromeDriver for the same major version
                echo "Exact version not found, falling back to latest ChromeDriver for Chrome $CHROME_MAJOR"
                CHROMEDRIVER_URL=$(curl -s "https://googlechromelabs.github.io/chrome-for-testing/latest-versions-per-milestone-with-downloads.json" | \
                  jq -r ".milestones.\"$CHROME_MAJOR\".downloads.chromedriver[] | select(.platform==\"linux64\") | .url")
              fi
              
              echo "Downloading ChromeDriver from: $CHROMEDRIVER_URL"
              wget -q "$CHROMEDRIVER_URL" -O chromedriver.zip
              unzip -q chromedriver.zip
              sudo mv chromedriver-linux64/chromedriver /usr/local/bin/
            else
              # Use old API for older Chrome versions
              echo "Using legacy ChromeDriver API for Chrome $CHROME_MAJOR"
              CHROMEDRIVER_VERSION=$(curl -s "https://chromedriver.storage.googleapis.com/LATEST_RELEASE_$CHROME_MAJOR")
              echo "Installing ChromeDriver version: $CHROMEDRIVER_VERSION"
              
              wget -q "https://chromedriver.storage.googleapis.com/$CHROMEDRIVER_VERSION/chromedriver_linux64.zip"
              unzip -q chromedriver_linux64.zip
              sudo mv chromedriver /usr/local/bin/
            fi
            
            sudo chmod +x /usr/local/bin/chromedriver
            
            # Verify installation
            chromedriver --version || {
              echo "ChromeDriver installation failed!"
              exit 1
            }
      
      - run:
          name: Verify Chrome and ChromeDriver
          command: |
            echo "Chrome version:"
            google-chrome --version || echo "Chrome not found"
            echo "ChromeDriver version:"
            chromedriver --version || echo "ChromeDriver not found"
            echo "ChromeDriver location:"
            which chromedriver || echo "ChromeDriver not in PATH"

            # Verify major versions match
            CHROME_MAJOR=$(google-chrome --version | awk '{print $3}' | cut -d'.' -f1)
            CHROMEDRIVER_MAJOR=$(chromedriver --version | awk '{print $2}' | cut -d'.' -f1)
            if [ "$CHROME_MAJOR" != "$CHROMEDRIVER_MAJOR" ]; then
              echo "ERROR: Chrome major version ($CHROME_MAJOR) != ChromeDriver major version ($CHROMEDRIVER_MAJOR)"
              exit 1
            fi
            echo "Version match confirmed: Chrome $CHROME_MAJOR, ChromeDriver $CHROMEDRIVER_MAJOR"
      
      - run:
          name: Create directories
          command: |
            mkdir -p $CIRCLE_ARTIFACTS $CIRCLE_TEST_REPORTS
            mkdir -p tests/output tests/screenshots
      
      - run:
          name: Wait for MongoDB
          command: |
            for i in {1..60}; do
              if nc -z mongo 27017; then
                echo "MongoDB is ready!"
                break
              fi
              sleep 5
            done
      
      - run:
          name: Install Meteor
          command: |
            if command -v meteor &> /dev/null && meteor --version 2>/dev/null; then
              echo "Meteor already installed (from cache)"
              meteor --version
            else
              echo "Installing Meteor fresh..."
              rm -rf ~/.meteor
              curl https://install.meteor.com/?release=3.3 | sh
            fi
            echo 'export PATH="$HOME/.meteor:$PATH"' >> $BASH_ENV
            source $BASH_ENV
            sudo ln -sf $HOME/.meteor/meteor /usr/local/bin/meteor
      
      - run:
          name: Prepare build environment
          command: |
            echo "Meteor local cache: $(du -sh .meteor/local 2>/dev/null || echo 'none')"
            echo "Node modules: $(du -sh node_modules 2>/dev/null || echo 'none')"
      
      - run:
          name: Verify code changes
          command: |
            echo "Checking for @uiw/color-convert in package.json..."
            grep -q "@uiw/color-convert" package.json && echo "✓ Found @uiw/color-convert" || echo "✗ Missing @uiw/color-convert"
            
            echo "Checking for business pages import in App.jsx..."
            grep -q "from './pages/index.business.js'" imports/ui/App.jsx && echo "✓ Found business pages import" || echo "✗ Missing business pages import"
            
            echo "Checking DiagnosticReport REST config..."
            grep -A5 '"DiagnosticReport"' settings/settings.honeycomb.tdd.json | grep -q '"interactions"' && echo "✓ Found DiagnosticReport REST config" || echo "✗ Missing DiagnosticReport REST config"
      
      - run:
          name: Install dependencies
          command: |
            # Safety net: patch lockfile for cross-platform rspack bindings
            # Handles the case where lockfile was regenerated on macOS without linux entries
            node -e "
            const fs = require('fs');
            const lock = JSON.parse(fs.readFileSync('package-lock.json', 'utf8'));
            const binding = lock.packages['node_modules/@rspack/binding'];
            if (binding && binding.optionalDependencies) {
              let patched = false;
              for (const [name, version] of Object.entries(binding.optionalDependencies)) {
                const key = 'node_modules/' + name;
                if (!lock.packages[key]) {
                  const os = name.includes('darwin') ? ['darwin'] : name.includes('linux') ? ['linux'] : name.includes('win32') ? ['win32'] : [];
                  const cpu = name.includes('x64') ? ['x64'] : name.includes('arm64') ? ['arm64'] : name.includes('ia32') ? ['ia32'] : name.includes('wasm32') ? ['wasm32'] : [];
                  lock.packages[key] = { version, cpu, dev: true, license: 'MIT', optional: true, os };
                  patched = true;
                  console.log('  Added: ' + key + '@' + version);
                }
              }
              if (patched) {
                fs.writeFileSync('package-lock.json', JSON.stringify(lock, null, 2));
                console.log('Lockfile patched with missing rspack platform entries');
              } else {
                console.log('All rspack platform entries already present in lockfile');
              }
            }
            "

            echo "Installing npm dependencies..."
            meteor npm install --verbose
            echo "npm install completed successfully"

            # Build the dcmjs bundle (build/ is gitignored in the submodule).
            # npm install reified the submodule's devDeps from the lockfile
            # (libraries/dcmjs/node_modules/* entries), so rollup is available
            # without pnpm.
            echo "Building dcmjs bundle..."
            (cd libraries/dcmjs && npx rollup -c)
            echo "dcmjs bundle built successfully"

            # meteor node (>=22.13) rather than the image's node 20 — the
            # suite imports the ESM bundle via module-syntax detection
            echo "Running dcmjs metadata extraction tests..."
            meteor node --test imports/ui/DICOM/utils/DcmjsMetadata.test.mjs

            # Verify rspack binding is available for this platform
            if node -e "require('@rspack/binding')" 2>/dev/null; then
              echo "✓ rspack binding loaded successfully"
            else
              echo "✗ FATAL: rspack binding not loadable"
              echo "Contents of node_modules/@rspack/:"
              ls -la node_modules/@rspack/ 2>/dev/null || echo "  (directory not found)"
              exit 1
            fi

            # Update browserslist database to avoid warnings
            echo "Updating browserslist database..."
            npx update-browserslist-db@latest || true

      - save_cache:
          key: meteor-install-v3.3-{{ arch }}
          paths:
            - ~/.meteor

      - run:
          name: Clear stale Meteor resolver cache
          command: |
            echo "Removing resolver-result-cache.json to prevent stale package references..."
            rm -f .meteor/local/resolver-result-cache.json

      - run:
          name: Pre-build Meteor packages
          no_output_timeout: 20m
          command: |
            echo "Pre-building Meteor packages..."
            echo "This step downloads and compiles Meteor packages to speed up the actual app start"
            
            # First, just download packages without building
            echo "Downloading Meteor packages..."
            meteor list || true
            
            # Run build with progress monitoring
            export EXTRA_WORKFLOWS="<< parameters.extra-workflows >>"
            (
              meteor build --directory /tmp/build-test --debug &
              BUILD_PID=$!
              
              # Monitor progress
              START_TIME=$(date +%s)
              while kill -0 $BUILD_PID 2>/dev/null; do
                CURRENT_TIME=$(date +%s)
                ELAPSED=$((CURRENT_TIME - START_TIME))
                echo "[$(date)] Meteor packages building... (${ELAPSED}s elapsed)"
                
                # Check package cache size
                if [ -d ".meteor/local" ]; then
                  echo "Package cache size: $(du -sh .meteor/local 2>/dev/null | cut -f1)"
                fi
                
                sleep 30
              done
              
              wait $BUILD_PID
              BUILD_EXIT=$?
              echo "Build completed with exit code: $BUILD_EXIT"
              exit $BUILD_EXIT
            ) || {
              echo "Pre-build timed out or failed, but continuing..."
              echo "This is expected for large projects"
            }
            
            # Clean up
            rm -rf /tmp/build-test
            echo "Pre-build completed"

      - save_cache:
          key: npm-deps-v4-{{ checksum "package-lock.json" }}
          paths:
            - node_modules
            - .meteor/local

      - run:
          name: Debug Meteor packages
          command: |
            echo "=== Meteor Package Information ==="
            echo "--- .meteor/packages ---"
            cat .meteor/packages
            echo ""
            echo "--- .meteor/versions ---"
            cat .meteor/versions
            echo "=== End Package Information ==="
      
      - run:
          name: Start Meteor
          background: true
          no_output_timeout: 30m
          command: |
            # Set DEV_AUTO_LOGIN based on parameter
            if [ "<< parameters.disable-auto-login >>" = "true" ]; then
              export DEV_AUTO_LOGIN=false
              export DEV_AUTO_USERNAME=""
              export DEV_AUTO_PASSWORD=""
            fi
            echo "Starting Meteor at $(date)..."
            # Explicitly disable SyncedCron for tests
            export ENABLE_SYNCED_CRON=false
            export TEST_RUN=true
            # Load npm workflow packages (replaces Atmosphere --extra-packages)
            export EXTRA_WORKFLOWS="<< parameters.extra-workflows >>"
            # Add signal handling and verbose output
            trap 'echo "Received signal at $(date)"; exit' SIGTERM SIGINT
            # Run Meteor with explicit settings and all ONC packages loaded
            # Add periodic output to prevent timeout
            (
              meteor run --settings settings/settings.honeycomb.tdd.json --port 3000 &
              METEOR_PID=$!

              # Output progress every 30 seconds to prevent timeout
              while kill -0 $METEOR_PID 2>/dev/null; do
                echo "[$(date)] Meteor is still starting up..."
                sleep 30
              done

              wait $METEOR_PID
            )
      
      - run:
          name: Wait for Meteor
          no_output_timeout: 25m
          command: |
            for i in {1..240}; do
              if curl -f http://localhost:3000 > /dev/null 2>&1; then
                echo "Meteor is ready after $((i * 5)) seconds!"
                exit 0
              fi
              echo "Waiting for Meteor... ($i/240, $((i * 5))s elapsed)"
              sleep 5
            done
            echo "ERROR: Meteor failed to start within 20 minutes"
            echo "Checking Meteor process:"
            ps aux | grep meteor || true
            exit 1
      
      # Security-critical endpoint layer smoke test (july-fix-now #3):
      # /api/rpc unary/validation/auth-negative/auth-positive/batch/notification
      # + /baseR4/metadata, via curl against the booted server. This is the
      # re-anchored coverage for issue #171 (meteortesting:mocha runs 0 tests
      # under the rspack build). Runs in every test-group: seconds of cost,
      # and a deliberate break of RpcAuth/JsonRpcEndpoint turns CI red.
      - run:
          name: Endpoint smoke tests (/api/rpc + /baseR4/metadata)
          command: ./scripts/endpoint-smoke-test.sh
      
      - run:
          name: Run Nightwatch tests - << parameters.group-name >>
          command: |
            # Debug information
            echo "Starting test execution at: $(date)"
            echo "Current directory: $(pwd)"
            echo "Node version: $(node --version)"
            echo "NPM version: $(npm --version)"
            
            # Verify Meteor is still running
            echo "Checking Meteor status..."
            if ! curl -f http://localhost:3000 > /dev/null 2>&1; then
              echo "ERROR: Meteor is not responding on port 3000"
              echo "Checking processes:"
              ps aux | grep meteor || true
              exit 1
            fi
            echo "Meteor is running"
            
            echo "========================================"
            echo "Running test group: << parameters.group-name >>"
            echo "Test files: << parameters.test-files >>"
            echo "========================================"
            
            # Start ChromeDriver in background
            echo "Starting ChromeDriver..."
            /usr/local/bin/chromedriver --port=9515 --log-level=WARNING &> /tmp/chromedriver.log &
            CHROMEDRIVER_PID=$!
            
            # Wait for ChromeDriver
            sleep 5
            
            # Check if ChromeDriver is running
            if ! ps -p $CHROMEDRIVER_PID > /dev/null; then
              echo "ChromeDriver failed to start"
              cat /tmp/chromedriver.log
              exit 1
            fi
            
            echo "ChromeDriver is running with PID: $CHROMEDRIVER_PID"
            
            # Run tests
            echo "Starting Nightwatch tests..."
            export NIGHTWATCH_EXTERNAL_CHROMEDRIVER=true
            
            # Run tests without timeout command (might be causing the termination)
            TEST_FILES="<< parameters.test-files >>"
            npx nightwatch --config nightwatch.circle.conf.js $TEST_FILES || TEST_EXIT_CODE=$?
            
            # Cleanup
            kill $CHROMEDRIVER_PID 2>/dev/null || true
            
            echo "Test execution completed at: $(date)"
            exit ${TEST_EXIT_CODE:-0}
          no_output_timeout: 20m
      
      
      - store_test_results:
          path: tests/output

      - store_artifacts:
          path: tests/screenshots
          destination: screenshots

  # Fast isomorphic-lib unit tests (node --test) — no browser, Mongo, or Meteor.
  # Complements the Nightwatch E2E groups above (FABLE-TECH-DEBT-PAYDOWN.md § P3).
  # NOTE: npmPackages/* + extensions/* are gitignored, so they are absent from a
  # bare monorepo checkout — this job runs whatever lib tests ARE present (core/*
  # here; everything in local dev). Nested-repo packages also run their own
  # `npm test` in their own per-repo CI.
  lib-unit-tests:
    docker:
      - image: cimg/node:20.11.0
    resource_class: small
    steps:
      - checkout
      - run:
          name: Isomorphic lib unit tests (node --test)
          command: npm run test:lib
      # Safety nets (july-fix-now #4) — bare-checkout, dependency-free:
      - run:
          name: ServerMethods registry core tests
          command: npm run test:rpc
      - run:
          name: WorkflowRegistry tests (component-override contracts)
          command: npm run test:workflow-registry
      - run:
          name: Workflow navigation tests
          command: npm run test:workflow-navigation
      - run:
          name: Workflow parser tests (barrel/loader emitters)
          command: npm run test:workflow-parser
      - run:
          name: global.Collections contract audit
          command: npm run audit:collections
      # ESM-subject unit tests (imports/lib + imports/ui/theme + npmPackages,
      # zero imports — bare-checkout safe). Subjects use `export` in .js, so
      # the scripts carry --experimental-detect-module for this job's node 20
      # (default-on ≥22).
      - run:
          name: SMART launch builders (PKCE)
          command: npm run test:smart-launch
      - run:
          name: Endpoint conformance probe
          command: npm run test:conformance-probe
      - run:
          name: Analytics path scrubbing (PHI)
          command: npm run test:analytics-scrub
      - run:
          name: Release-feed update check
          command: npm run test:update-check
      - run:
          name: Logger PHI redaction walker (bounded on arbitrary graphs)
          command: npm run test:logger-redact
      - run:
          name: Ambiance background-value helpers
          command: npm run test:background-value
      - run:
          name: Ambiance image analysis (focus/ink/scrim/palette)
          command: npm run test:ambiance-analysis
      - run:
          name: Directory search shadow (nameLower)
          command: npm run test:search-shadow
      - run:
          name: Org→Endpoint linkage matcher
          command: npm run test:org-linkage

  # Dedicated RPC method-test job (Stage 1, docs/RPC-TESTING.md): boots the
  # app against its OWN ephemeral Mongo sidecar, then runs the JSON-RPC test
  # suites in tests/rpc/ — the OpenRPC-driven conformance sweep in FULL mode
  # (read+write+destructive is safe here: the database dies with the job) plus
  # the hand-written method suites (CRUD round-trips, auth matrix). Also runs
  # the endpoint smoke battery, giving it the named status check. Ratchet
  # baseline: tests/rpc/conformance-known-failures.json (17 real handler bugs
  # found on the sweep's first run, burn-down tracked there).
  rpc-method-tests:
    docker:
      - image: cimg/node:20.11.0
        environment:
          TZ: "America/Chicago"
          NODE_ENV: test
          NODE_OPTIONS: --max_old_space_size=2048
          TOOL_NODE_FLAGS: --max_old_space_size=2048
          MONGO_URL: mongodb://mongo:27017/meteor_rpc_test
          ENABLE_AUTOPUBLISH: true
          DEV_AUTO_LOGIN: true
          DEV_AUTO_USERNAME: "demouser"
          DEV_AUTO_PASSWORD: "password2025"
          TEST_RUN: true
          METEOR_DISABLE_OPTIMISTIC_CACHING: 1
      - image: mongo:6.0
        name: mongo
    resource_class: large
    steps:
      - checkout
      - run:
          name: Init dcmjs submodule
          command: git submodule update --init libraries/dcmjs
      - restore_cache:
          keys:
            - meteor-install-v3.3-{{ arch }}
            - meteor-install-v3.3-
      - restore_cache:
          keys:
            - npm-deps-v4-{{ checksum "package-lock.json" }}
            - npm-deps-v4-
      - run:
          name: Install Meteor
          command: |
            if command -v meteor &> /dev/null && meteor --version 2>/dev/null; then
              echo "Meteor already installed (from cache)"
              meteor --version
            else
              echo "Installing Meteor fresh..."
              rm -rf ~/.meteor
              curl https://install.meteor.com/?release=3.3 | sh
            fi
            echo 'export PATH="$HOME/.meteor:$PATH"' >> $BASH_ENV
            source $BASH_ENV
            sudo ln -sf $HOME/.meteor/meteor /usr/local/bin/meteor
      - run:
          name: Install npm dependencies
          no_output_timeout: 20m
          command: |
            meteor npm install
            (cd libraries/dcmjs && npx rollup -c)
      - run:
          name: Clear stale Meteor resolver cache
          command: rm -f .meteor/local/resolver-result-cache.json
      - run:
          name: Start Meteor
          background: true
          no_output_timeout: 30m
          command: |
            export ENABLE_SYNCED_CRON=false
            export TEST_RUN=true
            export EXTRA_WORKFLOWS="@node-on-fhir/reference-app,@node-on-fhir/us-core,@node-on-fhir/pacio-core"
            (
              meteor run --settings settings/settings.honeycomb.tdd.json --port 3000 &
              METEOR_PID=$!
              while kill -0 $METEOR_PID 2>/dev/null; do
                echo "[$(date)] Meteor is still starting up..."
                sleep 30
              done
              wait $METEOR_PID
            )
      - run:
          name: Wait for Meteor
          no_output_timeout: 25m
          command: |
            for i in {1..240}; do
              if curl -f http://localhost:3000 > /dev/null 2>&1; then
                echo "Meteor is ready after $((i * 5)) seconds!"
                exit 0
              fi
              echo "Waiting for Meteor... ($i/240, $((i * 5))s elapsed)"
              sleep 5
            done
            echo "ERROR: Meteor failed to start within 20 minutes"
            exit 1
      - run:
          name: Endpoint smoke tests
          command: ./scripts/endpoint-smoke-test.sh
      - run:
          name: OAuth client_assertion signature verification (CR-1)
          command: npm run test:oauth-assertion
      - run:
          name: Patient-compartment authorization (CR-3)
          command: npm run test:patient-compartment
      - run:
          name: Write-sanitization / meta.security governance (CR-4)
          command: npm run test:write-sanitization
      - run:
          name: Secure production config assertion (CR-2)
          command: npm run test:secure-config
      - run:
          name: Outbound URL SSRF guard (CR-10)
          command: npm run test:outbound-url-guard
      - run:
          name: RPC method tests (conformance sweep FULL + method suites)
          command: RPC_SWEEP=all npm run test:rpc-methods


  # Secret scanning (july-fix-now #4): the July 1 audit found force-committed
  # secrets (Galaxy token, RSA private key); CodeQL covers SAST only. gitleaks
  # scans the checked-out tree on every pipeline. Allowlist: .gitleaks.toml.
  secret-scan:
    docker:
      - image: zricethezav/gitleaks:latest
    resource_class: small
    steps:
      - checkout
      - run:
          name: gitleaks (secret scanning)
          command: gitleaks detect --source . --no-git --redact --verbose

  # accessibility gate: jest-axe over the shared a11y primitives (jsdom, no Meteor
  # bundle). Locks the WCAG 2.1 AA structural gains — fails on any axe
  # violation or a11y test failure. See scripts/a11y-ci.js.
  accessibility-gate:
    docker:
      - image: cimg/node:20.11.0
    resource_class: medium
    steps:
      - checkout
      - run:
          name: Init dcmjs submodule
          # npm install needs the file:libraries/dcmjs target present
          command: git submodule update --init libraries/dcmjs
      - restore_cache:
          keys:
            - npm-deps-v4-{{ checksum "package-lock.json" }}
            - npm-deps-v4-
      - run:
          name: Install npm dependencies
          command: npm install
      - run:
          name: accessibility gate (jest-axe)
          command: npm run a11y:ci

  # onboarding: simulates a brand-new developer machine. Fresh checkout, the one
  # documented pre-step (submodule init), then a plain UNCACHED `npm install`
  # with no pnpm — the postinstall guard (scripts/postinstall-dcmjs.js) must
  # produce the dcmjs bundle on its own. The other jobs restore caches and
  # hand-build the bundle, so they never exercise the path a real
  # `git clone && npm install && meteor run` takes; this one does.
  onboarding:
    docker:
      - image: cimg/node:20.11.0
    resource_class: medium
    steps:
      - checkout
      - run:
          name: Ensure pnpm is absent (prove the pnpm-free path)
          command: |
            if command -v pnpm >/dev/null 2>&1; then
              echo "pnpm found at $(command -v pnpm) — removing so this job proves the pnpm-free path"
              sudo rm -f "$(command -v pnpm)"
            fi
            ! command -v pnpm
      - run:
          name: Init dcmjs submodule (the one documented onboarding step)
          # npm install needs the file:libraries/dcmjs target present before it
          # can even read the dependency tree — postinstall can't save this.
          command: git submodule update --init libraries/dcmjs
      - run:
          name: Plain npm install (uncached; postinstall must build dcmjs)
          no_output_timeout: 20m
          command: npm install
      - run:
          name: Assert dcmjs bundle exists and loads
          command: |
            test -s libraries/dcmjs/build/dcmjs.js
            test -s libraries/dcmjs/build/dcmjs.es.js
            node -e "const d = require('dcmjs'); if (!d || !d.data || !d.data.DicomMessage) throw new Error('dcmjs loaded but data.DicomMessage missing'); console.log('dcmjs OK:', Object.keys(d).join(', '));"

workflows:
  version: 2
  parallel-tests:
    jobs:
      # Fast lib unit tests (node --test) — runs alongside the E2E groups
      - lib-unit-tests

      # gitleaks secret scanning over the checked-out tree
      - secret-scan

      # dedicated RPC method tests: full conformance sweep + method suites
      # against a booted app with its own ephemeral database
      - rpc-method-tests

      # jest-axe accessibility gate over shared primitives (jsdom, no Meteor bundle)
      - accessibility-gate

      # clean-clone onboarding gate: uncached npm install must build dcmjs
      - onboarding

      # Run each test group in parallel
      - test-group:
          name: accounts
          group-name: accounts
          test-files: "tests/nightwatch/honeycomb/default/accounts.login.js tests/nightwatch/honeycomb/default/accounts.signup.js tests/nightwatch/honeycomb/default/accounts.viewports.js"
          disable-auto-login: true
      
      - test-group:
          name: actors
          group-name: actors
          test-files: "tests/nightwatch/honeycomb/enable_autopublish/crud.patients.js tests/nightwatch/honeycomb/enable_autopublish/crud.practitioners.js tests/nightwatch/honeycomb/enable_autopublish/crud.practitionerRoles.js tests/nightwatch/honeycomb/enable_autopublish/crud.careteams.js tests/nightwatch/honeycomb/enable_autopublish/crud.organizations.js"
      
      - test-group:
          name: clinical-history
          group-name: clinical-history
          test-files: "tests/nightwatch/honeycomb/enable_autopublish/crud.observations.js tests/nightwatch/honeycomb/enable_autopublish/crud.conditions.js tests/nightwatch/honeycomb/enable_autopublish/crud.encounters.js tests/nightwatch/honeycomb/enable_autopublish/crud.procedures.js tests/nightwatch/honeycomb/enable_autopublish/crud.bodystructures.js tests/nightwatch/honeycomb/enable_autopublish/crud.clinicalimpressions.js tests/nightwatch/honeycomb/enable_autopublish/crud.riskassessments.js"

      - test-group:
          name: autoimmune
          group-name: autoimmune
          test-files: "tests/nightwatch/honeycomb/enable_autopublish/crud.allergyintolerances.js tests/nightwatch/honeycomb/enable_autopublish/crud.immunizations.js"

      - test-group:
          name: pharmacy
          group-name: pharmacy
          test-files: "tests/nightwatch/honeycomb/enable_autopublish/crud.medications.js tests/nightwatch/honeycomb/enable_autopublish/crud.medicationrequests.js tests/nightwatch/honeycomb/enable_autopublish/crud.medicationadministrations.js"
      
      - test-group:
          name: radiology
          group-name: radiology
          test-files: "tests/nightwatch/honeycomb/enable_autopublish/crud.diagnosticreports.js tests/nightwatch/honeycomb/enable_autopublish/crud.imagingstudies.js"
      
      - test-group:
          name: care-management
          group-name: care-management
          test-files: "tests/nightwatch/honeycomb/enable_autopublish/crud.careplans.js tests/nightwatch/honeycomb/enable_autopublish/crud.nutritionintakes.js tests/nightwatch/honeycomb/enable_autopublish/crud.nutritionorders.js tests/nightwatch/honeycomb/enable_autopublish/crud.nutritionProducts.js tests/nightwatch/honeycomb/enable_autopublish/crud.servicerequests.js tests/nightwatch/honeycomb/enable_autopublish/crud.activitydefinitions.js"
      
      - test-group:
          name: scheduling
          group-name: scheduling
          test-files: "tests/nightwatch/honeycomb/enable_autopublish/crud.appointments.js tests/nightwatch/honeycomb/enable_autopublish/crud.schedules.js"
      
      - test-group:
          name: structured-data-capture
          group-name: structured-data-capture
          test-files: "tests/nightwatch/honeycomb/enable_autopublish/crud.questionnaires.js tests/nightwatch/honeycomb/enable_autopublish/crud.questionnaireresponses.js"
      
      - test-group:
          name: clinical-trials
          group-name: clinical-trials
          test-files: "tests/nightwatch/honeycomb/enable_autopublish/crud.researchstudy.js tests/nightwatch/honeycomb/enable_autopublish/crud.researchsubjects.js"
      
      - test-group:
          name: administrative
          group-name: administrative
          test-files: "tests/nightwatch/honeycomb/enable_autopublish/crud.auditevents.js tests/nightwatch/honeycomb/enable_autopublish/crud.consents.js tests/nightwatch/honeycomb/enable_autopublish/crud.tasks.js tests/nightwatch/honeycomb/enable_autopublish/crud.plandefinitions.js"

      - test-group:
          name: supply-chain
          group-name: supply-chain
          test-files: "tests/nightwatch/honeycomb/enable_autopublish/crud.devices.js tests/nightwatch/honeycomb/enable_autopublish/crud.locations.js tests/nightwatch/honeycomb/enable_autopublish/crud.supplydeliveries.js tests/nightwatch/honeycomb/enable_autopublish/crud.supplyRequests.js"

      - test-group:
          name: communications
          group-name: communications
          test-files: "tests/nightwatch/honeycomb/crud.messageheaders.js tests/nightwatch/honeycomb/enable_autopublish/crud.communications.js"

      - test-group:
          name: public-health
          group-name: public-health
          test-files: "tests/nightwatch/honeycomb/crud.measures.js tests/nightwatch/honeycomb/crud.measurereports.js"

      - test-group:
          name: library-sciences
          group-name: library-sciences
          test-files: "tests/nightwatch/honeycomb/enable_autopublish/crud.medias.js tests/nightwatch/honeycomb/enable_autopublish/crud.documentreferences.js"

      - test-group:
          name: framework
          group-name: framework
          test-files: "tests/nightwatch/honeycomb/enable_autopublish/simple-sorting-test.js tests/nightwatch/honeycomb/dev_auto_login/my.profile.js tests/nightwatch/honeycomb/default/localhost.js tests/nightwatch/honeycomb/enable_autopublish/crud.endpoints.js tests/nightwatch/honeycomb/theme/ambianceControls.js"
      
      # ONC Health IT Certification test groups
      - test-group:
          name: base-ehr
          group-name: base-ehr
          # ONC-cert tests exercise the package UIs, so load every package that owns a
          # Base EHR criterion route (45 CFR 170.102, HTI-1 final rule definition).
          extra-workflows: "@node-on-fhir/reference-app,@node-on-fhir/us-core,@node-on-fhir/pacio-core,@node-on-fhir/order-catalog,@node-on-fhir/implantable-devices,@node-on-fhir/secure-messaging,@node-on-fhir/quality-measures,@node-on-fhir/decision-support,@node-on-fhir/prescription-benefit"
          test-files: "npmPackages/order-catalog/tests/nightwatch/170.315.a.1.test.js npmPackages/order-catalog/tests/nightwatch/170.315.a.2.test.js npmPackages/order-catalog/tests/nightwatch/170.315.a.3.test.js npmPackages/reference-app/tests/nightwatch/170.315.a.5.test.js npmPackages/implantable-devices/tests/nightwatch/170.315.a.14.test.js npmPackages/pacio-core/tests/nightwatch/170.315.b.1.test.js npmPackages/prescription-benefit/tests/nightwatch/170.315.b.4.test.js npmPackages/decision-support/tests/nightwatch/170.315.b.11.test.js npmPackages/quality-measures/tests/nightwatch/170.315.c.1.test.js npmPackages/reference-app/tests/nightwatch/170.315.g.7.test.js npmPackages/reference-app/tests/nightwatch/170.315.g.9.test.js npmPackages/reference-app/tests/nightwatch/170.315.g.10.test.js npmPackages/secure-messaging/tests/nightwatch/170.315.h.1.test.js"
      


            